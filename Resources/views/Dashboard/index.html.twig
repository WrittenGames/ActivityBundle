{% extends 'CiscoSystemsActivityBundle::base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="span8">
            {% render 'CiscoSystemsActivityBundle:TimeKeeping:punch' with {'embedded': true} %}
        </div>
        <div class="span4 pull-right">
            {% render 'CiscoSystemsActivityBundle:Project:list' with {'embedded': true} %}
        </div>
    </div>
{% endblock %}
